import React from 'react';
import styles from './Logo.module.scss';

const Logo = () =>{

    return(
        <div>
            <p className={styles.Logo}>loopstudios</p>
        </div>
    )
}

export default Logo;